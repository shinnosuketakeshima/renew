# CGI テキスト表示エラー | 直ぐ確認チェックリスト

## 🔴 今起きていること
- postmail.html でフォーム送信
- → CGI の **テキストコード** がブラウザに表示される
- → フォーム処理 CGI が **実行されていない**

---

## ✅ 直ぐに確認すること（優先順）

### 1️⃣ check.cgi で実行可能か確認（2分）
以下 URL にアクセス：
```
https://be-intl.com/check.cgi
```

**結果**：
- ✅ CGI 設定情報が表示 → CGI 実行可能
- ❌ Perl コードが表示 → CGI 実行不可

### 2️⃣ FileZilla でパーミッション確認（3分）
1. FileZilla でサーバーに接続
2. `postmail.cgi` を右クリック → 「ファイルのプロパティ」
3. パーミッション確認：
   ```
   所有者: ☑ 読取 ☑ 書込 ☑ 実行
   グループ: ☑ 読取 ☐ 書込 ☑ 実行
   その他: ☑ 読取 ☐ 書込 ☑ 実行
   ```
   → 数字で表現すると **755** または **775**

### 3️⃣ ファイルの場所確認（1分）
FTP でルートディレクトリを開いて確認：
```
✅ postmail.cgi       ← ルート直下にある
✅ init.cgi           ← ルート直下にある
✅ check.cgi          ← ルート直下にある
```

---

## 🔧 解決手順

### 方法 A: FileZilla でパーミッション設定（推奨・簡単）

1. FileZilla でサーバー接続
2. **postmail.cgi** を右クリック
3. **ファイルのプロパティ** を選択
4. **パーミッション** タブで以下を設定：
   - [ Owner ] 読取 ✅ 書込 ✅ 実行 ✅
   - [ Group ] 読取 ✅ 書込 ☐  実行 ✅
   - [ Other ] 読取 ✅ 書込 ☐  実行 ✅
5. **OK** をクリック
6. **同じ設定を init.cgi と check.cgi にも適用**

### 方法 B: SSH でコマンド実行（高速）

ホスティング業者の SSH アクセス情報がある場合：

```bash
# サーバーに SSH 接続
ssh ユーザー@サーバーアドレス

# ドキュメントルートに移動
cd /（またはホスティング業者指定のルート）

# パーミッション設定
chmod 755 postmail.cgi init.cgi check.cgi

# 確認
ls -la postmail.cgi
# 結果: -rwxr-xr-x ... postmail.cgi （実行可能）
```

---

## 📋 確認項目チェック表

```
□ check.cgi にアクセスして CGI 実行可能か確認した
  ├─ ✅ 結果が表示された → CGI 実行可能
  └─ ❌ テキストが表示された → パーミッション設定が必要

□ postmail.cgi のパーミッションを確認した
  ├─ ✅ 755 または 775 に設定
  └─ ❌ 644 など → 755 に変更が必要

□ init.cgi と check.cgi のパーミッションも 755 に設定した

□ postmail.html の form action が正しいか確認
  ├─ ✅ action="./postmail.cgi"
  └─ ❌ 別パスになっていないか

□ 上記すべてやったのに動かない
  └─ ホスティング業者に問い合わせ
```

---

## 🆘 ホスティング業者への問い合わせ内容

**件名**: Perl CGI スクリプトが実行されない

**本文**：
```
お疲れ様です。

Perl CGI スクリプト（postmail.cgi）をアップロードしたのですが、
実行されずにテキストコードが表示されます。

■ 設定状況
- ファイル位置: ドキュメントルート直下
- ファイル: postmail.cgi, init.cgi, check.cgi
- パーミッション: 755 に設定済み
- 実行環境: Perl

■ 確認事項
1. ドキュメントルート直下で CGI 実行は可能ですか？
2. Perl の正確なパスはどこですか？（#! で指定するパス）
3. 特別な設定が必要ですか？

よろしくお願いします。
```

---

## ✅ 解決確認

修正後の確認手順：

1. **postmail.html にアクセス**
2. **フォームに以下を入力**：
   ```
   名前: テスト太郎
   ふりがな: てすとたろう
   メール: test@example.com
   メール（確認）: test@example.com
   郵便番号: 1000001
   ご住所１: 東京都
   ```
3. **「確認」ボタンをクリック**

**期待される動作**：
- ✅ 確認画面が表示される
- ✅ CGI がテキストではなく HTML を返している

---

## 参考ファイル
- [CGI-TROUBLESHOOT.md](CGI-TROUBLESHOOT.md) - 詳細なトラブルシューティング
- [DEPLOY.md](DEPLOY.md) - デプロイ全体ガイド
