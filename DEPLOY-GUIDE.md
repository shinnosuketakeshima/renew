# be-intl.com 本番環境デプロイガイド

本番サーバーへのデプロイ手順を詳しく説明します。

---

## 準備段階

### 1. デプロイファイル一覧の生成

PowerShell で以下を実行：

```powershell
cd C:\Github\renew
.\tools\list-deploy-files.ps1
```

結果：
- `tools/last-deploy-list.txt` - アップロード対象（1053ファイル）
- `tools/local-only-list.txt` - ローカル開発用（アップロード不要）
- `tools/deploy-summary.txt` - サマリー

### 2. SEO 監査の実行

```powershell
.\tools\seo-audit.ps1
```

結果に「no ERROR-level issues」と表示されることを確認。

### 3. サイトマップの生成（オプション）

```powershell
.\tools\generate-sitemap.ps1
```

`sitemap.xml` を生成（必要に応じて）。

---

## FTP クライアント設定

### **推奨：FileZilla を使用**

#### インストール
- [FileZilla 公式サイト](https://filezilla-project.org/)からダウンロード
- Windows 用スタンドアロン版をインストール

#### 接続設定

1. FileZilla を開く
2. メニュー：**ファイル** → **サイトマネージャー**
3. 新規サイトを作成：
   ```
   ホスト：your-ftp-host.com
   プロトコル：FTP
   ユーザー名：your-ftp-username
   パスワード：your-ftp-password
   ポート：21
   ```
4. **接続** をクリック

---

## ファイルアップロード

### **ステップ1：接続確認**

FileZilla でサーバーに接続すると、左側（ローカル）と右側（リモート）にファイルツリーが表示されます。

### **ステップ2：アップロード対象の確認**

ローカル側（左）で `C:\Github\renew` に移動。

**以下をアップロード：**

| ファイル/フォルダ | 説明 | モード |
|---|---|---|
| `*.html` | すべての HTML ファイル | テキスト |
| `assets/` | CSS、JS、画像 | テキスト（CSS/JS）、バイナリ（画像） |
| `postmail/` | CGI フォーム（完全なディレクトリ） | バイナリ |
| `SriLankaPhotos/` | 画像 | バイナリ |
| `NepalPhotos/` | 画像 | バイナリ |

**アップロード不要：**
- ❌ `tools/` ディレクトリ
- ❌ `docs/` ディレクトリ
- ❌ `.cursor/` ディレクトリ
- ❌ `.git/` ディレクトリ
- ❌ `.claude/` ディレクトリ
- ❌ `README.md`
- ❌ `.env*` ファイル
- ❌ `.python-version`
- ❌ `supabase/` ディレクトリ（Function はすでにデプロイ済み）

### **ステップ3：HTML ファイルのアップロード**

1. ローカル側で、`index.html` を選択
2. 右クリック → **アップロード**
3. または、ドラッグ&ドロップ

**重要：テキストモードでアップロード**

FileZilla 設定：
- **編集** → **設定** → **転送設定**
- 転送モード：**バイナリ**（自動）を選択
- または、ファイル右クリック → **アップロード（テキストモード）**

繰り返し：すべての `*.html` をアップロード

### **ステップ4：assets/ ディレクトリのアップロード**

1. ローカル側で `assets/` フォルダを右クリック
2. **アップロード** を選択
3. ファイルサイズが大きいので、時間がかかることを確認

**ファイル形式別のモード：**
- `*.css` → テキストモード
- `*.js` → テキストモード
- `*.webp`, `*.jpg`, `*.png`, `*.gif` → バイナリモード

FileZilla は自動で判定します。

### **ステップ5：画像フォルダのアップロード**

`SriLankaPhotos/` と `NepalPhotos/` を **バイナリモード** でアップロード。

### **ステップ6：postmail/ ディレクトリのアップロード（重要）**

```
postmail/
  ├── postmail.cgi      ← CGI スクリプト（権限 755）
  ├── init.cgi
  ├── check.cgi
  ├── lib/              ← Perl モジュール
  ├── tmpl/             ← HTML テンプレート
  └── data/             ← サーバー生成ファイル
```

1. `postmail/` 全体を **バイナリモード** でアップロード
2. 以下のステップで権限を設定

---

## CGI ファイルの権限設定（重要）

### **ステップ1：CGI ファイルの権限確認**

FileZilla でアップロード後：

1. リモート側（右側）で `postmail/` を開く
2. `postmail.cgi` を右クリック
3. **ファイル属性** を選択
4. 権限を確認

### **ステップ2：権限を 755 に変更**

1. **ファイル属性** ダイアログで：
   - **所有者：** r(読), w(書), x(実行) にチェック
   - **グループ：** r(読), x(実行) にチェック
   - **その他：** r(読), x(実行) にチェック
2. **OK** をクリック

数値で表すと **755** です。

### **ステップ3：すべての .cgi ファイルに適用**

以下のファイルに同じ権限を設定：
- `postmail.cgi` ✅
- `init.cgi` ✅
- `check.cgi` ✅

### **ステップ4：CGI 実行確認**

ブラウザで以下にアクセス：

```
https://be-intl.com/postmail/check.cgi
```

**期待される結果：**
- CGI が正常に実行される
- Perl スクリプトのエラーメッセージが表示される（スクリプトが読まれた証拠）

**エラーの場合：**
- ❌ 500 エラー → Perl シェバンパス（`#!/usr/local/bin/perl`）が間違っている
- ❌ テキストが表示される → ファイル権限が 755 に設定されていない

---

## アップロード後の確認

### **ステップ1：ホームページへのアクセス**

```
https://be-intl.com/
```

表示確認：
- ✅ ヘッダー、ナビゲーションが表示される
- ✅ ヒーロー画像が表示される
- ✅ チャットウィジェットボタン（右下）が表示される

### **ステップ2：主要ページの確認**

以下のページにアクセス：

```
https://be-intl.com/srilanka.html
https://be-intl.com/nepal.html
https://be-intl.com/program.html
https://be-intl.com/voices.html
https://be-intl.com/faq.html
```

確認項目：
- ✅ ページが正常に表示される
- ✅ 画像が読み込まれている
- ✅ CSS が適用されている
- ✅ チャットウィジェットが表示される

### **ステップ3：チャットウィジェットの確認**

1. ホームページを開く
2. 右下のチャットボタン（💬）をクリック
3. 「体験者の経験から探す」パネルが表示される
4. 質問を入力して送信
5. レスポンスが返ってくることを確認

### **ステップ4：リンク確認**

各ページで以下を確認：
- ✅ ナビゲーション内のリンクが動作する
- ✅ 「資料請求」ボタンが `postmail.html` にリンクしている
- ✅ フッターの各リンクが機能する

### **ステップ5：フォーム確認**

`https://be-intl.com/postmail.html` にアクセス：
- ✅ フォームが表示される
- ✅ フォーム送信が機能する（確認メール等）

### **ステップ6：SEO チェック**

ブラウザの開発者ツール（F12）を開いて：

**Console タブ：**
- ❌ エラーメッセージがないか確認
- ⚠️ 警告は問題なし

**Network タブ：**
- ✅ 404 エラーがないか確認（画像やリソースが見つからない）

### **ステップ7：モバイル表示確認**

開発者ツール → **デバイスツールバー** でモバイル表示を確認：
- ✅ スマートフォン（375px幅）で正常に表示される
- ✅ タブ操作が可能
- ✅ チャットウィジェットが表示される

---

## トラブルシューティング

### **画像が表示されない**

**原因：** 画像ファイルのアップロード漏れまたはパスが間違っている

**対策：**
1. ローカルで `SriLankaPhotos/` フォルダを確認
2. サーバーで同じフォルダが存在するか確認
3. FileZilla で画像ファイルを再度アップロード

### **CSS が適用されない**

**原因：** `assets/css/` フォルダがアップロードされていない

**対策：**
1. `assets/` フォルダ全体をテキストモードでアップロード
2. サーバーで `assets/css/home.css` が存在するか確認

### **チャットウィジェットが動作しない**

**原因：** 
1. `assets/js/taiken-chat-widget.js` がアップロードされていない
2. Supabase Function がアクセス不可

**対策：**
1. `assets/js/` フォルダをアップロード
2. ブラウザ開発者ツール（F12）→ Network タブで、Supabase API への接続を確認
3. Supabase Function が ACTIVE か確認（ダッシュボード → Functions）

### **フォームが送信できない**

**原因：** `postmail/` ディレクトリがアップロードされていない、または権限が 755 でない

**対策：**
1. `postmail/` フォルダがサーバーに存在するか確認
2. `postmail.cgi` の権限を 755 に設定
3. `https://be-intl.com/postmail/check.cgi` にアクセスして CGI が実行可能か確認

### **500 エラーが出る**

**原因：** 
1. CGI ファイルのシェバンパスが間違っている（`#!/usr/local/bin/perl`）
2. Perl モジュールが見つからない

**対策：**
1. サーバー管理者に Perl の場所を確認（通常 `/usr/bin/perl` または `/usr/local/bin/perl`）
2. `postmail.cgi` の先頭行を修正

### **301 リダイレクトエラー**

**原因：** 古いページへのアクセスで、リダイレクト設定が見つからない

**対策：**
1. サーバーの `.htaccess` で 301 リダイレクトを設定
2. 例：`Redirect 301 /old-page.html https://be-intl.com/new-page.html`

---

## デプロイ確認チェックリスト

アップロード完了後、以下をすべて確認してください：

- [ ] ホームページが表示される
- [ ] 画像が読み込まれている
- [ ] CSS が適用されている
- [ ] チャットウィジェットが表示される
- [ ] チャットウィジェットが機能する（質問を送信できる）
- [ ] ナビゲーションリンクが動作する
- [ ] 「資料請求」ボタンが機能する
- [ ] フォーム送信が機能する
- [ ] モバイル表示で正常に表示される
- [ ] ブラウザコンソールにエラーがない
- [ ] Network タブに 404 エラーがない
- [ ] CGI（check.cgi）が実行可能

---

## デプロイ後の注意

### **ファイル更新時**

以下の場合はアップロードが必要：

| ファイル | 更新頻度 | アップロード方法 |
|---|---|---|
| HTML ファイル | 随時 | テキストモード |
| CSS ファイル | 随時 | テキストモード |
| JavaScript | 随時 | テキストモード |
| 画像 | 随時 | バイナリモード |
| CGI スクリプト | まれ | バイナリ + 権限 755 |

### **バックアップ**

本番環境へのアップロード前に、サーバー上の既存ファイルをバックアップすることを推奨します。

---

## サポート連絡先

問題が発生した場合、サーバー管理者に確認が必要な事項：

- FTP ホスト、ユーザー名、パスワード
- Perl のパス（通常 `/usr/local/bin/perl` または `/usr/bin/perl`）
- CGI 実行許可（`.cgi` ファイルの実行が許可されているか）
- `.htaccess` の使用可否

---

**最後に：本番環境へのアップロード前に、すべての確認が完了していることを確認してください。**
